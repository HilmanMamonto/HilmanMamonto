import React from "react";
import PropTypes from "prop-types";
import "./styles.scss";

const NavMobile = ({ className }) => {
  return <div className="nav-mobile">nav mobile</div>;
};

NavMobile.propTypes = {};

export default NavMobile;
