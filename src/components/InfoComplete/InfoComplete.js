import React from "react";
import PropTypes from "prop-types";

const InfoComplete = (props) => {
  return <div className="rounded bg-white">complete</div>;
};

InfoComplete.propTypes = {};

export default InfoComplete;
